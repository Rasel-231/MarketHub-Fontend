import { PaymentFail } from '@/components/modules/components/Transaction/PaymentFail/fail';
import React from 'react';

const page = () => {
    return (
        <div>
            <PaymentFail/>
        </div>
    );
};

export default page;